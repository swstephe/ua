h1 My Latest Dives
.row(ng-controller='diveLogCtrl')
  .col-sm-4(ng-repeat="dive in dives | orderBy: 'site'")
    h3 {{dive.site}}
    h3 {{dive.location}}
    h3 {{dive.depth}} feet | {{dive.time}} min
